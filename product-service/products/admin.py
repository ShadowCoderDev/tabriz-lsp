from django.contrib import admin

# Note: Django admin doesn't work directly with mongoengine documents
# This is a placeholder. For MongoDB admin, consider using mongoengine's admin or a custom admin interface.
